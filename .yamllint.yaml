---
extends: default

ignore: |
  .copier-answers.yml
  .cache/
  .tox/

rules:
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable
  line-length: disable
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
